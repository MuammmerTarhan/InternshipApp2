const config = {
  backendURL: 'YOUR_BACKEND_URL',
  apiVersion: 'v1', // Example API version
  apiKey: 'YOUR_API_KEY', // Example API key
  maxRetryAttempts: 3, // Example number of maximum retry attempts for API calls
  // Add more configuration values as needed
};

export default config;
